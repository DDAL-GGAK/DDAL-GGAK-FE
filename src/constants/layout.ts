export const SIDE_NAV = Object.freeze({
  WIDTH: 108 as const,
  BORDER_WIDTH: 1 as const,
  GAP: 24,
});

export const TOP_NAV = Object.freeze({
  HEIGHT: 100 as const,
  BORDER_WIDTH: 1 as const,
});

export const NAVLINK = Object.freeze({
  WIDTH: 60 as const,
  HEIGHT: 60 as const,
  BORDER_RADIUS: 10 as const,
});
